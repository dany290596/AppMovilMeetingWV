import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crcdemexico.WebVisitsMeeting',
  appName: 'WebVisitsMeeting',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
